// Configuration for Brain Web extension
// Update these URLs to match your backend and frontend deployment
// For production, update these to your actual domain URLs
// For development, localhost URLs are fine

const CONFIG = {
  // Backend API URL - update for production deployment
  BACKEND_URL: "http://localhost:8000",
  // Frontend URL - update for production deployment  
  FRONTEND_URL: "http://localhost:3000"
};

// Note: In production, these should be set to your actual domain:
// BACKEND_URL: "https://api.yourdomain.com"
// FRONTEND_URL: "https://yourdomain.com"

