// Configuration for Brain Web extension
// Update these URLs to match your backend and frontend ports

const CONFIG = {
  BACKEND_URL: "http://localhost:8000",
  FRONTEND_URL: "http://localhost:3000"
};

